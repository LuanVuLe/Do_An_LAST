<Window x:Class="Do_An.GiaoDien_Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Dashboard - StarEnglish"
        Height="650" Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize">

    <Window.Resources>
        <!-- Style đã được chuyển từ NVQL sang Admin để đồng bộ hóa -->

        <!-- Nút bo góc chung -->
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar item (Màu Cyan đồng bộ với NVQL) -->
        <Style x:Key="SidebarListItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="4,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5CE1E8"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#008BA0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Nút đăng xuất (Màu Đỏ đồng bộ với NVQL) -->
        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E63946"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="38"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Thanh bên (Sidebar - Màu Cyan) -->
        <DockPanel Grid.Column="0" Background="#0098B0">

            <!-- Header/Logo -->
            <TextBlock Text="StarEnglish Admin" DockPanel.Dock="Top" Foreground="White"
                       FontSize="22" FontWeight="Bold" Margin="20,25,0,35"/>

            <!-- Menu List -->
            <ListBox x:Name="MenuList" DockPanel.Dock="Top" Background="#0098B0"
                     BorderThickness="0" Foreground="White" FontSize="16"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ItemContainerStyle="{StaticResource SidebarListItem}"
                     SelectionChanged="MenuList_SelectionChanged">
                <ListBoxItem Content="🏠 Trang chủ" IsSelected="True"/>
                <ListBoxItem Content="👤 Quản lý tài khoản"/>
                <ListBoxItem Content="📚 Quản lý lớp học"/>
                <ListBoxItem Content="🔒 Quản lý phân quyền"/>
                <ListBoxItem Content="🛡 Bảo mật hệ thống"/>
                <ListBoxItem Content="📊 Xem báo cáo"/>
            </ListBox>

            <!-- Footer (Nút Thoát/Đăng xuất) -->
            <StackPanel DockPanel.Dock="Bottom" Margin="12">
                <Button Content="🚪 Đăng xuất"
                        Style="{StaticResource LogoutButtonStyle}"
                        Click="BtnThoat_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- Nội dung chính -->
        <ScrollViewer Grid.Column="1" Background="#F5F7FA" VerticalScrollBarVisibility="Auto">
            <!-- Thêm x:Name="ContentPanel" vào StackPanel/Grid để code C# truy cập -->
            <StackPanel x:Name="ContentPanel" Margin="35,30"/>
        </ScrollViewer>
    </Grid>
</Window>
