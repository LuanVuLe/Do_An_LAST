<UserControl x:Class="Do_An.Admin_QLPQ"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Background="#f5f7fa">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="QUẢN LÝ PHÂN QUYỀN" 
                   FontSize="26" FontWeight="Bold" 
                   Foreground="#2c3e50" Margin="0 0 0 15"/>

        <!-- Thanh tìm kiếm + nút -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBox x:Name="txtSearch" Width="250" Height="35"
                     VerticalContentAlignment="Center"
                     Padding="10"
                     Background="White"
                     BorderBrush="#d1d5db"
                     BorderThickness="1"
                     FontSize="14"
                     ToolTip="Nhập tài khoản cần tìm..."
                     TextChanged="txtSearch_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                                        <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <Button x:Name="btnLuu" Content="💾 Lưu thay đổi"
                    Width="130" Height="35" Margin="10 0 0 0"
                    Background="#4CAF50" Foreground="White"
                    BorderBrush="#4CAF50" FontWeight="Bold"
                    Cursor="Hand" Click="btnLuu_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="1"
                                            CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Bảng dữ liệu -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="10" 
                BorderBrush="#e5e7eb" BorderThickness="1" >
            <DataGrid x:Name="dgPhanQuyen"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#f9fafb"
                      GridLinesVisibility="None"
                      FontSize="14"
                      ItemsSource="{Binding}">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#3498db"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="35"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Tài khoản" Binding="{Binding TenTaiKhoan}" Width="*"/>
                    <DataGridComboBoxColumn Header="Quyền" SelectedItemBinding="{Binding Quyen}" Width="200">
                        <DataGridComboBoxColumn.ItemsSource>
                            <x:Array Type="{x:Type sys:String}">
                                <sys:String>Quản trị</sys:String>
                                <sys:String>Nhân viên</sys:String>
                                <sys:String>Giáo viên</sys:String>
                            </x:Array>
                        </DataGridComboBoxColumn.ItemsSource>
                    </DataGridComboBoxColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
